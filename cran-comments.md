## R CMD check results

0 errors | 0 warnings | 1 note

* This is a resubmission. Changes since version 0.1.1:
  - Replaced all `\dontrun{}` with `\donttest{}` in examples as requested.
  - Simplified examples to execute in < 5 seconds.
  - Added references to DESCRIPTION field (ggplot2 and plotly DOIs).
  - Verified `on.exit()` is correctly used in zzz.R for option changes.
  - Added `@seealso` references to vignettes for additional examples.
